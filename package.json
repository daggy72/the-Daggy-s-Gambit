{
  "name": "the-daggys-gambit",
  "version": "1.0.0",
  "description": "A personal interactive chess course. The app itself is dependency-free; npm is only used for tests.",
  "private": true,
  "scripts": {
    "test": "node --test tests/engine.test.js tests/content.test.js",
    "smoke": "node tests/smoke.spec.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
